<div class="container">
  <h1><%= @pet.name %></h1>
  <div id = "pets">
    <ul></ul>
      <li>  <%= image_tag("#{@pet.image_url}", :height => "400")%> </li>
      <li> Description: <%= @pet.description %></li>
      <li> Species: <%= @pet.species %></li>
      <li> <b> Price to rent: $<%= @pet.price %> </b> </li>
      <br>
  </div>
    <%= link_to "Book Pet", pet_bookings_path(@pet), method: :post %>
</div>
