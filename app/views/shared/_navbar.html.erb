<div class="navbar border-bottom navbar-expand-sm navbar-light navbar-lewagon">

  <div class="container">


    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
      <%# <div class="container-fluid"> %>
    <%# <a class="navbar-brand" href="#"> %>
      <h1 class="h1-navbar">Crazy Pets</h1>
    <%# </a> %>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
        <% if user_signed_in? %>
          <li class="nav-item active">
            <%= link_to "Home", pets_path, class: "nav-link" %>
          </li>
          <li class="nav-item active">
            <%= link_to "My Pets", my_pets_path(current_user), class: "nav-link"  %>
          </li>
          <li class="nav-item active">
            <%= link_to "My Bookings", my_bookings_path(current_user), class:"nav-link"  %>
          </li>
          <li class="nav-item active">
            <%= link_to "Log out", destroy_user_session_path, method: :delete, class:"nav-link"  %>
          </li>
    </div>
        <% else %>
          <li class="nav-item btn-flat">
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
